# SQLBot Configuration
PROJECT_NAME="SQLBot"
# 本地模型路径配置
# 将模型路径设置为实际的backend目录
LOCAL_MODEL_PATH=D:\\mynj\\SQLBot\\backend

# 默认嵌入模型（保持默认值）
DEFAULT_EMBEDDING_MODEL=text2vec-base-chinese

# Backend
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173,https://localhost,https://localhost:5173"
# 安全警告: SECRET_KEY 是用于JWT签名的密钥，必须保密且唯一！
# 请立即生成一个新的随机密钥并替换下面的值
# 生成方法: python -c "import secrets; print(secrets.token_urlsafe(32))"
# 重要: 每个部署环境必须使用不同的SECRET_KEY，切勿使用默认值或公开的密钥
SECRET_KEY=VCzmavK0pvnXJ89W6_IENxvRaFa1oGqEnsvomGTTD2Y

DEFAULT_PWD="FXmynj@2025"

LOG_LEVEL="DEBUG"  # DEBUG, INFO, WARNING, ERROR
SQL_DEBUG=False

# MySQL 配置
# SQLBOT_DB_URL=mysql+pymysql://root:123456@:127.0.0.1:3306/faxuan
CACHE_TYPE="memory"


# Postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=sqlbot
POSTGRES_USER=root
POSTGRES_PASSWORD=Password123@pg # Change this to your pwd

# SERVER_IMAGE_HOST=http://192.168.1.112:8001/images/